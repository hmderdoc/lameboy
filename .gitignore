# Build artifacts
/target/

# ROMs and per-user saves: large and/or copyrighted — never commit.
# (.saves lives under roms/, so this covers it too.)
/roms/

# Deployed binary (build artifact produced by `cargo build --release`)
/terminal_gameboy

# Sysop's live door config (ships only as lameboy.ini.example)
/lameboy.ini

# ANSI capture dumps (local debugging scratch, not part of the source)
/menu.bin
/music_cap.bin

# APC audio diagnostics log (runtime artifact, truncated each session)
/apc_audio.log
